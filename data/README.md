# data

Not tracked.  `raw/` holds sources as downloaded, `derived/` the files the
model reads, each with a `.receipt.edn` naming its source, and `db/` the
Datahike evidence store.  See `../DATA.md` for every source, its licence and
how to obtain it.
